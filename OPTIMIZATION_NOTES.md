# 钢铁前线：整体优化说明

本版本是在原项目基础上完成的核心系统优化版，保留原有地图、武器、载具、波次、爆破和 UI 结构。

## 本次已完成

### 1. 敌人与友军 AI

- 新增 `js/navigation.js`，提供共享二维网格 A* 寻路。
- 敌人不再只在出生点附近随机巡逻，会主动向最近未占领据点推进。
- 敌人会在玩家和存活友军之间选择可见目标，而不是永远只攻击玩家。
- 丢失目标后会前往最后目击位置搜索，再恢复推进。
- AI 卡住时会清除旧路径并重新规划。
- 敌人巡逻、推进、侧移、后撤均使用实际速度驱动走路动画。
- 友军会定时重新索敌，目标被墙遮挡后停止隔墙射击。
- 多名友军会分配不同据点，不再全部挤向目标数组中的第一个据点。
- 修复友军命中后缺失 `enemy` 引用导致的运行时错误。
- 友军新增受伤、死亡和移除流程。

### 2. 地图与碰撞

- 修复大战场旋转长墙碰撞盒被错误扩大成巨型正方形的问题。
- 修复竞技场实例化箱子先写位移再重置矩阵，造成模型与碰撞体位置不一致的问题。
- 导航网格按照角色半径扩张障碍，减少贴墙、穿角和卡死。

### 3. 武器光影与射击

- 步枪、手枪、狙击枪模型启用 `receiveShadow`。
- 降低过强的全局环境填充光，增强明暗区域差异。
- 根据玩家头顶和太阳方向是否被建筑遮挡，动态调整枪械环境反射强度。
- 统一保存武器材质基础环境反射值，避免加载 M4A1 后材质参数被二次覆盖丢失。
- 射击改为两段射线：先从准星确定目标点，再从真实枪口发射；枪口被墙挡住时无法穿墙命中。

### 4. 刷怪与战线

- 取消“每次都选择离玩家最远的同一个刷新点”。
- 刷新点会参考当前战线、玩家距离和出生点拥挤程度。
- 敌人出生后直接进入推进状态，并在感知范围内切换到追击或攻击。

## 验证结果

运行：

```bash
npm test
```

包含原项目全部无头测试，以及新增的优化回归测试：

- 大战场细长墙体碰撞盒验证。
- A* 绕墙路径验证。
- 敌人主动推进模拟。
- 友军命中链路验证。

测试模拟中，远端敌人在 20 秒内向战线推进约 74 米。

本地 HTTP 服务器已验证以下资源均能正常返回：

- `/index.html`
- `/js/main.js`
- `/js/navigation.js`
- `/js/enemy.js`
- `/js/allies.js`
- `/js/weapons.js`
- `/assets/models/m4a1.obj`

当前处理环境禁止 Chromium 访问本地 HTTP 地址，因此未能在这里重新执行自动点击、锁定鼠标和实际游玩截图。建议在 Windows 上运行 `start.bat` 后，重点实机检查枪械阴影强度、复杂建筑入口寻路和不同友军数量下的性能。

## 主要修改文件

- `js/main.js`
- `js/enemy.js`
- `js/allies.js`
- `js/weapons.js`
- `js/maps/arena.js`
- `js/maps/battlefield.js`
- `js/navigation.js`（新增）
- `tests/test-optimization.mjs`（新增）
- `tests/e2e-test.py`
- `package.json`
